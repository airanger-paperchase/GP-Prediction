
# Minimal requirements needed to run the backend API in this repo.
# If you need SQL Server connectivity inside the container, uncomment pyodbc
# and use the alternate Dockerfile that installs the ODBC driver.

fastapi
uvicorn[standard]
pandas
numpy
openpyxl  # optional: needed only if you upload Excel (.xls/.xlsx) files
python-dotenv
jsonschema
openai
python-multipart  # needed for file uploads (form data)
pyodbc

# Optional/extra (commented out):
# pyodbc  # enable if you need to connect to SQL Server from inside the container
gradio

tenacity
