fastapi==0.95.2
uvicorn[standard]==0.22.0
pydantic==1.10.9
pandas==2.2.3
numpy==1.26.4
pyodbc==4.0.35
python-dotenv==1.0.0
openai==1.33.0
jsonschema==4.25.1
typing-extensions==4.8.0
requests==2.31.0
azure-identity
azure-keyvault-secrets

# required for FastAPI form/file parsing
python-multipart==0.0.6

# Pin httpx to a version compatible with the OpenAI client used here to avoid
# unexpected keyword argument errors (e.g. 'proxies') when the library
# signatures change in newer httpx releases.
httpx==0.23.3

# used in some scripts but not strictly required to run the API endpoints
openpyxl==3.1.2
